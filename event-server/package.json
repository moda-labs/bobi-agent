{
	"name": "event-server",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"core"
	],
	"scripts": {
		"build:local": "esbuild src/local.ts --bundle --platform=node --target=node20 --outfile=dist/local.js --metafile=dist/local.meta.json --legal-comments=eof",
		"start:local": "node dist/local.js",
		"test": "vitest"
	},
	"dependencies": {
		"@moda-labs/bobi-events-core": "*",
		"ws": "^8.18.0"
	},
	"devDependencies": {
		"@chat-adapter/slack": "^4.30.0",
		"@types/node": "^20.11.0",
		"@types/ws": "^8.5.0",
		"esbuild": "^0.25.0",
		"typescript": "^5.7.2",
		"vitest": "~3.2.0"
	}
}
