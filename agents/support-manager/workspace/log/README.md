# Daily support logs

One file per day: `<YYYY-MM-DD>.md`, written by the support-manager. The
daily-report workflow reads today's file. Safe to delete old files. These
dated logs are runtime output (and may reference customer mail) — they are
not committed.
