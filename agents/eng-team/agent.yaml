version: "1.1.0"

entry_point: director
chat: slack

services:
  - name: github
    events: true
  - name: slack
    events: true
  - name: linear
    events: true

slack:
  bot_token: ${SLACK_BOT_TOKEN}

linear:
  api_key: ${LINEAR_API_KEY}
