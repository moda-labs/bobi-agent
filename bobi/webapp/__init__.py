"""The unified Bobi web app (`bobi app`) — dashboard, onboarding, and chat.

One machine-scoped app over the whole `$BOBI_HOME/agents/` tree (#525). Runs
as a background daemon by default, like the agents themselves.
"""
