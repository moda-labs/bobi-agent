Groups an operational screen without adding chrome.

```jsx
<SectionLabel action={<span style={{ fontFamily: "var(--font-mono)", fontSize: 11, color: "var(--text-secondary)" }}>6 shown</span>}>
  Events
</SectionLabel>
<div className="bobi-card">…</div>
```

Sentence case, 13.5px, ink. Uppercase mono is for page titles and table headers only.
