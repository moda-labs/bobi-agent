Bounded config choice.

```jsx
<Select label="Runtime" options={["claude-code", "openai-codex"]} hint="runs on your existing plan" />
<Select label="Approvals" options={[{ label: "inline (in thread)", value: "inline" }, { label: "dashboard", value: "dashboard" }]} />
```
